import PeripheralsCatalog from "@/components/peripherals-catalog"

export default function PeripheralsPage() {
  return (
    <main className="min-h-screen bg-background pt-20">
      <PeripheralsCatalog />
    </main>
  )
}
